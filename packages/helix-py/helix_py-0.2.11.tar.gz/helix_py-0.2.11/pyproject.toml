[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["helix/"]

[project]
version = "0.2.11"
name = "helix-py"
authors = [{ name = "HelixDB Team", email = "lukasnitzsche@yahoo.com"}]
description = "HelixDB Python library for easy rag agents"
readme = "README.md"
requires-python = ">=3.9"
liscense = { file = "LICENSE" }
dependencies = [
    "numpy >= 2.0.2",
    "pyarrow >= 19.0.1",
    "tqdm >= 4.67.1",
]

[project.urls]
Homepage = "https://www.helix-db.com/"
Documentation = "https://docs.helix-db.com/"
Issues = "https://github.com/HelixDB/helix-py/issues"

