[project]
name = "dongzc1025-tobe-time-mcp"
version = "0.1.1"
description = "The MCP server to get local time"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastmcp>=2.7.0",
]

[tool.uv.workspace]
members = ["venv"]

[project.scripts]
dongzc1025-tobe-time-mcp = "tobe_time_mcp.server:main"