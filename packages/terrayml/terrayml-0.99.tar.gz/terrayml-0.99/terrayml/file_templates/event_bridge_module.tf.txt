module "event_bridge" {
  source = "./modules/event_bridge"
  common = local.common
  event_bridge_rule_list = ${EVENT_BRIDGE_RULE_LIST}
  event_bridge_event_bus_list = ${EVENT_BRIDGE_EVENT_BUS_LIST}

}