{
    "function_name": "${LAMBDA_FUNCTION_NAME}",
    "api_gateway_name": "${LAMBDA_API_GATEWAY_NAME}",
    "path": "${LAMBDA_PATH}",
    "method": "${LAMBDA_METHOD}",
    "invoke_arn": "module.lambda_functions.lambda_invoke_arns[\"${LAMBDA_FUNCTION_NAME}\"]",
    "authorizer_type": "${LAMBDA_AUTHORIZATION_TYPE}",
    "authorizer_name": "${LAMBDA_AUTHORIZATION_NAME}",
    "authorization_scopes": "${LAMBDA_AUTHORIZATION_SCOPES}"
}