aws_account_id="${AWS_ACCOUNT_ID}"
aws_profile="${AWS_PROFILE}"
aws_region = "${AWS_REGION}"
environment = "${ENVIRONMENT}"
project_name = "${PROJECT_NAME}"
project_code = "${PROJECT_CODE}"
service_name = "${SERVICE_NAME}"
app_path = "${TERRAYML_PATH}"