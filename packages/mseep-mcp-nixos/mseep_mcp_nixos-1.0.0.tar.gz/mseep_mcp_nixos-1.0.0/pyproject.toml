[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[tool.black]
line-length = 120

[tool.flake8]
max-line-length = 120
ignore = [
    "E402",
    "E203",
]

[tool.pytest.ini_options]
testpaths = [
    "tests",
]
python_files = "test_*.py"

[tool.hatch.build.targets]
