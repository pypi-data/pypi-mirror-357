[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kapil_bhai"
version = "0.1.0"
description = "its about kapill bhai"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
    { name="Prasoon", email="prasoonrajput4218@gmail.com" }
]
dependencies = [
    "requests"  # or other dependencies
]

[project.urls]
Homepage = "https://github.com/yourusername/mypackage"
