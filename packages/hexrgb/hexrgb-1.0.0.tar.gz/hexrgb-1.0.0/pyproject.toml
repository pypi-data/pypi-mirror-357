[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hexrgb"
version = "1.0.0"
description = "Convert HEX color code to RGB."
authors = [
    { name = "Mallik Mohammad Musaddiq", email = "mallikmusaddiq1@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.6"

[project.scripts]
hexrgb = "hexrgb.main:main"