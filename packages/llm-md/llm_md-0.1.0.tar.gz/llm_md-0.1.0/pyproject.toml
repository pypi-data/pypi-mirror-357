[project]
name = "llm-md"
version = "0.1.0"
description = "CLI tool for generating LLM context from GitHub repositories"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.2.1",
    "pathspec>=0.12.1",
]

[project.scripts]
llm-md = "llm_md.cli:main"
