[tool.poetry]
name = "arcane-criteo"
version = "1.0.6"
description = "A client to help us request Criteo API"
authors = ["Arcane <product@wearcane.com>"]
packages = [
    { include = "arcane" }
]

[tool.poetry.dependencies]
python = "^3.6"
backoff = ">=1.10.0"
criteo-api-marketingsolutions-sdk = "2023.1.0.230224"

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
