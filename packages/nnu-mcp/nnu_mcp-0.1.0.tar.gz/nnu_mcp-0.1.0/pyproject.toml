[project]
name = "nnu-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.4",
    "requests>=2.32.4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/nnu_mcp"]

[project.scripts]
nnu-mcp = "nnu_mcp:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"