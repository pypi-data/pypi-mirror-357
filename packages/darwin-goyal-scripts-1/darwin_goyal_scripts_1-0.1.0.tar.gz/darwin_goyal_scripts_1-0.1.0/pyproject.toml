[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "darwin_goyal_scripts_1"
version = "0.1.0"
description = "A test Python package"
readme = "README.md"
authors = [
  { name="Your Name", email="you@example.com" }
]
license = { text = "MIT" }
requires-python = ">=3.7"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
