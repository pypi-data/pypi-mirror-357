[project]
name = "live_studio_open_service"
version = "0.1.0"
description = "A simple socket-based RPC client"
readme = "README.md"
authors = [{ name = "kanglibo", email = "kanglibo@bytedance.com" }]
license = { text = "MIT" }
requires-python = ">=3.7"
dependencies = []

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
