[project]
name = "z-y-y-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.9.4",
]

[project.scripts]
z-y-y-mcp-demo = "z_y_y_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
