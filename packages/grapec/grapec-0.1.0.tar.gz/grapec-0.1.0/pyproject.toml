[project]
name = "grapec"
version = "0.1.0"
description = "Load protobuf and grpc definition file dynamiclly"
readme = "README.md"
authors = [
    { name = "AN Long", email = "aisk1988@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "grpcio-tools>=1.73.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
