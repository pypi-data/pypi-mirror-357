[project]
name = "owasp.untrust.boxedpath"
version = "0.1.0"
description = "Secure path sandboxing for untrusted environments"
authors = [{ name = "Yariv Tal", email = "yariv@owasp.org" }]
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["owasp.untrust.boxedpath"]
