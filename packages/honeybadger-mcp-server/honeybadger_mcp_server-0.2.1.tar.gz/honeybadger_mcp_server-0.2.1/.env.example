# Required configuration
HONEYBADGER_API_KEY=your-api-key-here
HONEYBADGER_PROJECT_ID=your-project-id-here

# Optional configuration
TRANSPORT=sse  # Transport protocol: 'sse' or 'stdio'
HOST=127.0.0.1 # Host to bind to when using SSE transport
PORT=8050      # Port to listen on when using SSE transport
LOG_LEVEL=INFO # Logging level: DEBUG, INFO, WARNING, ERROR, or CRITICAL
