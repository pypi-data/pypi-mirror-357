[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-sqlalchemy"
version = "0.0.1"
description = "A test package for checking PyPI name availability"
readme = "README.md"
authors = [{name = "simple-sqlalchemy", email = "test@example.com"}]
license = {text = "MIT"}
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
requires-python = ">=3.6"

[project.urls]
Homepage = "https://github.com/test/test-package"
