version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: examples/plivo-chatbot/Dockerfile
    restart: always
    ports:
      - "8765:8765"
    volumes:
      - ./examples/plivo-chatbot:/app
    command: python server.py
