[tool.poetry]
name = "arcane-bigquery-storage"
version = "1.1.2"
description = "A package to use the BigQuery Storage Write API"
readme = "README.md"
authors = ["Arcane <product@wearcane.com>"]
packages = [
    { include = "arcane" }
]

[tool.poetry.dependencies]
python = "^3.9"
# arcane-core = "1.7.0" (optional)
google-cloud-bigquery-storage = "^2.28.0"
backoff = ">=1.10.0"

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
