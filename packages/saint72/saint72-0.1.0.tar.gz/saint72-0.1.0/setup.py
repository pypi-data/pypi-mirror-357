from setuptools import setup, find_packages

setup(
    name="saint72",
    version="0.1.0",
    description="Генератор файла с кодом в корне проекта",
    author="Ваше Имя",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)
