[project]
name = "valenceai"
version = "0.5.1"
description = "Python SDK for Valence Vibrations Emotion Detection API"
authors = [{name = "Valence Dev Team", email = "shannon@valencevibrations.com"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["requests", "tqdm"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"