[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cm_test_config"
version = "0.2.0"
authors = [
  { name="SaginRavi", email="Sagin.Ravi@zeb.co" },
]
description = "A test configuration package"
readme = "README.md"
license = {text = "MIT"}  # ← THIS IS THE CRITICAL ADDITION
requires-python = ">=3.6"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",  
    "Operating System :: OS Independent",
]