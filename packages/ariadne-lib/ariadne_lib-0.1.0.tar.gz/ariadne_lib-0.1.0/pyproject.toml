[project]
name = "ariadne-lib"
authors = [
    {name = "Tony Chen"}
]
version = "0.1.0"
description = "Find your way in a maze of experiments"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
dev-dependencies = [
]

[build-system]
requires = ["hatchling >= 1.2.7"]
build-backend = "hatchling.build"

[project.scripts]
ariadne = "ariadne:cli"

[tool.hatch.build.targets.wheel]
packages = ["ariadne"]