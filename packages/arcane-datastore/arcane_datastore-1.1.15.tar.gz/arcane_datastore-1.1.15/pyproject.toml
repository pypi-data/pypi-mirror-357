[tool.poetry]
name = "arcane-datastore"
version = "1.1.15"
description = "Override datastore client"
readme = "README.md"
authors = ["Arcane <product@wearcane.com>"]
packages = [
    { include = "arcane" }
]

[tool.poetry.dependencies]
python = "^3.7"
google-cloud-datastore = "^2.11.0"
backoff = ">=1.10.0"
arcane-core = "^1.6.0"

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
