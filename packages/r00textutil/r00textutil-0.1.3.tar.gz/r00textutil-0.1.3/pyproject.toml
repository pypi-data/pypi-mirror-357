[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/textutil"]

[project]
name = "r00textutil"
version = "0.1.3"
requires-python = ">=3.12"
dependencies = [
    "r00logger",
    "r00system",
    "tqdm",
    "rich",
]

