[project]
name = "remote-mcp-cli"
version = "0.1.1"
description = "Minimal MCP client CLI"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "click",
    "httpx",
]

[project.scripts]
remote-mcp-cli = "remote_mcp_cli:cli"

