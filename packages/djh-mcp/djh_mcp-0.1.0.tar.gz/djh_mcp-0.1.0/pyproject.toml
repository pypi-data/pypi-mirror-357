[project]
name = "djh-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dartou", email = "" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.9.4",
]

[project.scripts]
djh-mcp = "djh_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
