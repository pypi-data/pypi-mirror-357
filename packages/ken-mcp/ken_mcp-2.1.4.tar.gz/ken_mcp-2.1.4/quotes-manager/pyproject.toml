[project]
name = "quotes-manager"
version = "0.1.0"
description = "MCP server for: Create an MCP for managing "favorite quotes" with features to add quotes like "To be or not to be" a..."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["fastmcp>=0.1.0", "python-dotenv"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
