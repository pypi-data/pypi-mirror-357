[project]
name = "multi-api-integration"
version = "0.1.0"
description = "MCP server for: Create an MCP that integrates with multiple APIs (GitHub, Slack, Discord) and handles webhook authen..."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["fastmcp>=0.1.0", "python-dotenv"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
