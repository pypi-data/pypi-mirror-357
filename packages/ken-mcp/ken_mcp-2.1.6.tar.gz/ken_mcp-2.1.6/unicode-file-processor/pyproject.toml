[project]
name = "unicode-file-processor"
version = "0.1.0"
description = "MCP server for: Build an MCP that processes files with unicode characters (ñ, 中文, emoji 🚀), handles very long file p..."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["fastmcp>=0.1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
