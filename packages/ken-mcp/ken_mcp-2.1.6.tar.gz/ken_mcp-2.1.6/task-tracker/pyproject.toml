[project]
name = "task-tracker"
version = "0.1.0"
description = "MCP server for: Create an MCP for task management with these features:
- Add tasks with title and description
- Mark..."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["fastmcp>=0.1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
