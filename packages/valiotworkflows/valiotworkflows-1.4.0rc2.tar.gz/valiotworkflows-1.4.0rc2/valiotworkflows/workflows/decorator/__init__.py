"""
Workflow Decorator Module

Usage:
    from valiotworkflows.workflows.decorator import workflow

Available Decorator:
    - workflow: Let us define workflows for Valiot,
        including their states, events, and other attributes.

Example:
    ```
    @workflow(defn=WorkflowDefinition(...))
    def my_workflow_function():
        pass
    ```
"""
