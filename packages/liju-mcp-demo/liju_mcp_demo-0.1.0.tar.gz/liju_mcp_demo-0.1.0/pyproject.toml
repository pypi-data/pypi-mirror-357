[project]
name = "liju-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "luliju", email = "luliju@kanzhun.com" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.9.4",
]

[project.scripts]
liju-mcp-demo = "liju_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
