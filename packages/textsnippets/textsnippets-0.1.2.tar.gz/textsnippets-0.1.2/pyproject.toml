[project]
name = "textsnippets"
version = "0.1.2"
description = "A library for extracting and preparing code and text snippets from various file types, primarily intended for consumption by Large Language Models (LLMs)."
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.uv]
package = true

[project.scripts]
textsnippets = "textsnippets.cli:main"