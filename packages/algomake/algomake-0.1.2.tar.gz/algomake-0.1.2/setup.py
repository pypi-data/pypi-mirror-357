# setup.py
from setuptools import setup, find_packages

setup(
    # This file is now primarily used for backward compatibility and
    # for `find_packages()`. Most metadata is in pyproject.toml.
    name="algomake",
    packages=find_packages(),
)