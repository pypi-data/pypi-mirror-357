[project]
authors = [{ name = "StasStrukov" }]

dependencies = ["pyyaml (==6.0.2)", "requests (>=2.32.4,<3.0.0)"]
description = ""
license = { text = "MIT" }
name = "strukov-adv-python-2025"
readme = "README.md"
requires-python = ">=3.11"
version = "0.1.1"

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core>=2.0.0,<3.0.0"]

[tool.poetry.group.dev.dependencies]
black = "^25.1.0"
ruff = "^0.11.13"
mypy = "^1.16.0"

[tool.poetry]
packages = [{ include = "strukov_adv-python-2025", from = "src" }]
