[project]
name = "slurm_emission"
version = "0.1.0"
authors = [
    { name = "Luca Herrtti", email = "luca.herrtti@gmail.com" }
]
description = "Submit jobs to SLURM seamlessly"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
keywords = ["slurm", "jobs", "emission", "hpc", "sbatch", "supercomputer", "cluster"]
license = "MIT"
license-files = ["LICEN[CS]E*"]
dependencies = [
    # This will be read from `requirements.txt` in the dynamic setup process
]

[project.urls]
Homepage = "https://github.com/lucehe/slurm-emission"
Issues = "https://github.com/lucehe/slurm-emission/issues"
