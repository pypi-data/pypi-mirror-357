[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "mcp_server",
]

[tool.pyright]
include = [
    "ifly_spark_agent_mcp",
]
venvPath = "."
venv = ".venv"
