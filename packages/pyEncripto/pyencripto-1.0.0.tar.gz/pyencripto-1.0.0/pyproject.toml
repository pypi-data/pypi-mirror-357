[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyEncripto"
version = "1.0.0"
authors = [{ name = "Tishkin Andrei", email = "atishkin060705@gmail.com" }]
description = "pyEncripto — a tool for encrypting Python projects and securely running them with encrypted modules and assets"
readme = "README.md"
requires-python = ">=3.10.0"
classifiers = [
   "Programming Language :: Python :: 3",
   "License :: OSI Approved :: MIT License",
   "Operating System :: OS Independent",
]
dependencies = ["pycryptodome >=3.23.0"]

[project.urls]
Homepage = "https://github.com/andrei1112111/pyEncripto"
Issues = "https://github.com/andrei1112111/pyEncripto/issues"

[project.scripts]
pyEncripto = "pyEncripto.cli:main"
