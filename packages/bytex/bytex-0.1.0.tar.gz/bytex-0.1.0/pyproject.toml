[project]
name = "bytex"
version = "0.1.0"
description = "Pydantic-like binary validation"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = []

[project.optional-dependencies]
test = ["pytest==8.3.5"]
examples = ["scapy==2.6.1"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
